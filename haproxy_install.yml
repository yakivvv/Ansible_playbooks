
---
- hosts:
    - all
#  become: true
#  sudo: True
#  sudo_user: ayakovlev
  become: yes
  become_user: root
  become_method: sudo
  tasks:
     - name: Install list of packages (Haproxy)
       apt:
        name: haproxy
        state: present
#        state: absent
